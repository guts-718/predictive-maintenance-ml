
# baseline pleateau --

| Model | RMSE   |
| ----- | ------ |
| RF    | ~17    |
| LSTM  | ~13–15 |

<!-- ## the predictive maintenance task that we are dooinghere is basically , going to be extrapolated to something that is using temporal logic instead of considering only the single row
- row what we would be doing next is to use LSTM AND grus instead of just using normal basic machine learning models like RANDOM FOREST, XG boost and stuff like that -->